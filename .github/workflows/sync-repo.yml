name: Sync Repo 1 to Repo 2

on:
  push:
    branches:
      - main  # Trigger the action on a push to the main branch of Repo 1

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      # Step 1 — Checkout the latest code from Repo 1
      - name: Checkout Repo 1
        uses: actions/checkout@v3

      # Step 2 — Set up Git configuration with user details
      - name: Setup Git
        run: |
          git config --global user.name 'Nassaritos'
          git config --global user.email 'ibraheem.a.nassar@gmail.com'

      # Step 3 — Clone Repo 2
      - name: Clone Repo 2
        run: git clone https://github.com/Nassaritos/karima_nadeem_wedding.git repo2

      # Step 4 — Copy files from Repo 1 to Repo 2, excluding .git
      - name: Copy changes to Repo 2
        run: |
          rsync -av --delete --exclude='.git' ./ repo2/
          cd repo2
          git add .
          git commit -m "Sync update from Repo 1"

      # Step 5 — Push changes to Repo 2 using GITHUB_TOKEN
      - name: Push changes to Repo 2
        run: |
          cd repo2
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/Nassaritos/karima_nadeem_wedding.git
